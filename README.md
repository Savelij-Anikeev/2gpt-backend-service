# 2GPT Backend Service

This NodeJS/ExpressJS application allows you to work with `GPT4` and Google `Gemini` in convenient way, using only one api for both ai platforms

# How to use

run 

    npm run dev
